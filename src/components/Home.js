import React from 'react'
import { Button } from './Button';

const Home = (props) => {
    
    return (
      <div>
        <Button color="red">push</Button>
        <Button color="green">pull</Button>
        <Button color="purple">legs</Button>
      </div>
    );
}

export default Home

